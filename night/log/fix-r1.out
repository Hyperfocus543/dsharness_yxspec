Commit `bc4496c` 落盘，只动了一个文件。报告如下：

## 修复报告

**文件**：`gateway/lib/stages.mjs` — `resolveStage()`（阶段识别，`server.mjs` 派活入口）

**问题**：旧实现按 STAGES 顺序做裸 `prompt.includes(stage.command)` 子串匹配，有两个确定性误派：
1. `/yxspec:swe-coding-verify-pc-v2`（PC 变体）被其前缀 `/yxspec:swe-coding-verify-v2` 先命中 → 派活跑到 `swe_coding_verify` token，产物扫描/状态回写/驾驶舱点亮全错位；
2. `/yxspec:swe-release-promote` 被 `/yxspec:swe-release` 误吞（promote 阶段实际跑成 release）。

**修复**：改为边界感知匹配（命令后必须跟空白/标点/结尾）+ 按命令长度降序匹配（同时提及多命令时取最具体的），保持「咨询问句含命令名不触发执行」的既有语义。

**验证**：
- 10 例边界冒烟全过（PC 变体/双命令/前缀+后缀/咨询问句均正确）；
- 27 个真实命令自命中回归通过（旧逻辑确认存在 1 处 promote 碰撞）；
- `npx tsc --noEmit` 0 error；`npm test` 33/33 通过。
- 单 commit `bc4496c`，未触碰其他工作区改动（stage-mapping.ts 的未提交删除为先前会话遗留，保持原样）。
