# yxspec-studio 工作进度总结（2026-08-29 → 2026-08-31）

> 人工工作进度文档。记录 yxspec-studio（React/Vite 前端 + Node 网关）在 8/29 至 8/31 的
> 手动功能开发 + 夜间自主优化产出 + 基础设施修复。运行产物见 `night/SUMMARY-night2-*.md`（gitignore 忽略）。

---

## 一、手动功能开发（8/29，按林汉飞需求）

| 需求 | 交付内容 |
|---|---|
| **对话框尺寸返工** | 中央执行终端：整体固定 ~20% 屏高（180–240px）、**留白区不可拖**；仅输入区（textarea）可拉高/压低（2–10 行、localStorage 记住偏好）。两轮返工定稿：①60% 整体可拖→否；②40% 留白可拖→否；③**留白固定 + 只调输入区 + 20%**→定稿 |
| **轨迹功能增强** | 轨迹聚合 model / goals / todos / userInputs / 推理摘要；**独立「轨迹」功能页**（navigation + functionCards 注册）+ 驾驶舱内嵌保留；成本页保留现状 |
| **轨迹调研** | dsh 官方 47 事件类型 vs 本地能力差异报告 |
| **自迭代双模式** | 框架优化（`score_core.py --eval-framework`）/ 产物优化（score_aggregate）双模式 |
| **git 工作区创建** | `git init` 本地仓库创建（白名单 + 目录/URL 安全校验 `isSafeTargetDir`/`isSafeGitUrl`）|
| **字号加大** | 全局梯级上调（11.5/12.5/13.5px @layer utilities 覆盖）|
| **持续一直跑改造** | night2 无限轮（END_AT 默认空、stop-flag 唯一停止通道）+ 崩溃自愈看门狗脚本 + 按日日志归档 |

## 二、夜间自主优化产出（8/29 晚 → 8/31，无人值守）

- **总提交**：约 **210 个**（99 文件改动），全部过 `gates.sh` 六项门禁后 commit+push
- **类型分布**：fix 124 · feat 57 · chore 4 · night2 自动 24
- **子系统分布**：git-workspace 67 · self-iteration 28 · tool-guard 26 · git-workspaces 18 · trajectory 7 · cost 3

### 主要成果

**git 工作区（git-workspace / git-workspaces，85 提交）**
- 操作结果摘要：push「推送 N 个提交」、fetch「落后 N→M」、checkout「前后分支/游离态」、clone 进度秒数
- 分支增强：按远端分组（本地在前）、上游 ahead/behind 偏差（↑N/↓M）、stash 速览、脏文件 +N/-M 改动统计
- 安全加固：`isSafeTargetDir` 归一判盘符根（`.` 段形变拦截）、clone/init 目标目录预拦截、clone 超时兜底
- 交互修复：操作留痕区块、失败行内持久错误态、空态 CTA、按钮 operating 态、切换下拉空白修复、写操作后缓存失效

**self-iteration（28 提交）**
- 续跑预填：轮数/目标/模式/「断点恢复」按 run-state 精确预填（不再被驾驶舱当前阶段覆盖）
- 表单增强：派活命令实时预览、执行中锁定、取消反馈、已执行时长、启动后聚焦轮次瀑布
- 评分/排序：评分子分构成（Master/Stage）、阶段权威 flow 序（修静默退化）、`resolveStageToken` 兜底下划线归一

**tool-guard（26 提交，安全）
- 漏网修复：命令替换/子shell 递归扫描、行继续符绕过、wrapper 尾随实参、config/stash 只读子命令细分放行
- 误伤修复：tag 只读 flag 组合、config 裸键查询、`-z` NUL 输出开关、remote get-url

**其他**：轨迹×git 融合、成本角标自刷新、门控徽标证据 tooltip、阶段 tag 可读化

## 三、基础设施与故障处理（8/31）

### night2 崩溃排查与修复
- **现象**：8/31 00:31 runner 崩停（112 轮后），无看门狗 → 一直停着
- **根因 1（CRLF）**：`runner-night2.sh` 被工具链落成 CRLF，`set -u` 下多行 `local` 续行崩溃
- **根因 2（local 自引用，真根因）**：`local DAY_DIR="$(date...)" OUT="$DAY_DIR/..."` 中 bash 先展开全部 RHS，`$DAY_DIR` 未赋值即被 `set -u` 拦（MSYS bash 特有）→ **拆成独立 `local` 声明**
- **修复**：转 LF + 拆分 local + 新增 `.gitattributes` 强制 `*.sh eol=lf` + gitignore 匹配按日 `SUMMARY-night2*.md`
- **验证**：重启后稳定跑 8 小时（00:33 → 08:24，43 轮），stop-flag 正常停

### 其他
- 修 `pollTask` 超时单测 flake（30ms→300ms，CI 满载时只轮询 1 次断言失败）
- 看门狗计划任务**未注册**（按用户要求暂缓）；`night/register-night2-watchdog.bat` 已就绪待管理员运行

## 四、当前状态

- night2 已优雅停止（stop-flag），工作树干净，所有改动推送 `main`
- 待办：①看门狗注册（想真正"一直跑"需开）；②vault 已归档 3 条笔记（schtasks 踩坑/终端语义/夜间无限轮）

---

*生成：2026-08-31 08:26 · yxspec-studio-release*
